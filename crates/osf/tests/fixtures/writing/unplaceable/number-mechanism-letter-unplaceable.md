The outage traced back to mechanism (b), a race between two workers writing the same row. We still need to patch the retry logic before the next deploy.

<!-- osf-unplaceable
label: unplaceable
kind: number
target: mechanism (b)
reason: bracketed letter with no list item in the paragraph that defines it
-->
