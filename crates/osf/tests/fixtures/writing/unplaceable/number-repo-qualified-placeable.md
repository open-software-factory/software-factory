The login crash is now closed in acme/widgets#125 (the login crash). Support can point the customer at that reference directly.

<!-- osf-unplaceable
label: placeable
kind: number
reason: repository-qualified number carries its own bracketed description
-->
