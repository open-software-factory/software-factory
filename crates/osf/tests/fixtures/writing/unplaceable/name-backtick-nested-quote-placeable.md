That hook returns `{"decision":"block","reason":"why"}` to the caller, unchanged.

<!-- osf-unplaceable
label: placeable
kind: name
target: decision
reason: a double-quoted span nested inside a backtick code span is code, not a mention
-->
