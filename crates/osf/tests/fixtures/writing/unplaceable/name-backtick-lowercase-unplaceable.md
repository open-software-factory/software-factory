The rollout used a `dark launch` for the new pricing page, gated behind a flag. No customer outside the pilot list could see the new price yet.

<!-- osf-unplaceable
label: unplaceable
kind: name
reason: lowercase term in backticks with no code characters, never defined
-->
