We pinned the renderer to moon 2.5.5 after the earlier upgrade broke the export path. That version number is not a reference to anything else in the project.

<!-- osf-unplaceable
label: placeable
kind: number
reason: a word and a number that form a version number, not a candidate
-->
