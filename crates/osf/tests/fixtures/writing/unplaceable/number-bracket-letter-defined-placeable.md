The outage traced back to mechanism (b). Three mechanisms were on the list:

- (a) a missing lock
- (b) a race between two workers writing the same row
- (c) a stale cache read

<!-- osf-unplaceable
label: placeable
kind: number
reason: a list item in the same paragraph starts with the bracketed letter
-->
