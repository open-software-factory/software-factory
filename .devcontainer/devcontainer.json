{
  "name": "Open Software Factory",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "remoteUser": "dev",
  "postCreateCommand": "osf --version && moon --version && node --version && pnpm --version && git config --show-origin core.hooksPath"
}
