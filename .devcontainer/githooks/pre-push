#!/bin/sh
# git push gives this hook the remote name and URL as arguments and the
# ref updates on stdin; osf verify --checkpoint pre-push needs neither.
exec /opt/factory/bin/osf verify --checkpoint pre-push
